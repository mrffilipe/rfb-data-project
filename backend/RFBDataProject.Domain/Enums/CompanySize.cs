namespace RFBDataProject.Domain.Enums;

/// <summary>
/// UI filter codes for company size (distinct from raw RFB porte_empresa values).
/// </summary>
public enum CompanySize
{
    Mei = 1,
    Micro = 2,
    Small = 3,
    Medium = 4,
    Large = 5
}
